package com.example;

public class Cal {

	public static int findMax(int arr[]){  
        int max=arr[0];  //0 index assume as max
        for(int i=1;i<arr.length;i++){  
            if(max<arr[i])  
                max=arr[i];  
        }  
        return max;  
    }  
}
